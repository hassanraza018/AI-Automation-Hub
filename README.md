# AI-Automation-Hub
An automated AI-powered weather bot built using n8n that fetches weather data from OpenWeatherMap API, processes it using Gemini AI, and sends clean weather updates via Gmail every 12 hours.
